'use babel';

import CreateReactComponentView from '../lib/create-react-component-view';

describe('CreateReactComponentView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
